import { DropdownMenu as ShadcnDropdownMenu } from "../ui/dropdown-menu";
export const DropdownMenu = (
  props: React.ComponentProps<typeof ShadcnDropdownMenu>
) => {
  return <ShadcnDropdownMenu {...props} />;
};
